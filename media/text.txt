document.getElementById("modal").addEventListener("click", function (e) {
        const isModalContent = e.target.closest("#modalContent");
        const isArrow = e.target.closest(".nav");
        if (!isModalContent && !isArrow) {
            closeModal();
        }